# Desktop CLS fallback final specification review

Date: 2026-07-19 (Asia/Shanghai)  
Scope: read-only final acceptance review of the planned desktop-only fallback. No product source, dependency, configuration, or Git commit was changed by this review.

## Verdict: APPROVED

All five acceptance requirements pass for the reviewed implementation. The code and regression tests prove the intended pending-state and breakpoint contracts; the completed project validation also records successful targeted, full-suite, lint, build, and diff checks. This approval does **not** claim an immediate numerical CrUX improvement: CrUX CLS is 28-day field data, so a local browser session cannot prove its field value has already declined.

## Acceptance results

| Requirement | Result | Verified evidence |
| --- | --- | --- |
| 1. Desktop `>=1280px` keeps the `120px` preload and retains an equal-height, overflow-contained pending fallback | **PASS** | The desktop media query is exactly `(min-width: 1280px)` and the existing margin remains `120px 0px` at `src/components/layout/DeferredEditorLayout.tsx:46-47, 101-120`. Only the desktop dynamic entry defines `loading` at `:39-42`. Its fallback root is a full-width shell with `min-h-[100svh]`, `xl:h-screen`, and `xl:overflow-hidden` at `:27-36`; the resolved editor root has the same `min-h-[100svh]`, `xl:h-screen`, and `xl:overflow-hidden` desktop containment at `src/components/layout/EditorLayout.tsx:46-52`. Focused tests retain the exact observer margin (`DeferredEditorLayout.test.tsx:201-208`), exercise the unresolved desktop pending state (`:210-226`), and cover direct desktop hash hydration (`:166-199`). Fresh review command: `pnpm exec vitest run src/components/layout/DeferredEditorLayout.test.tsx --reporter=verbose` exited 0 (18/18 tests). |
| 2. Mobile initial state, explicit launch, direct URL/hash, timer, and observer behavior remain unchanged; mobile has no loading fallback | **PASS** | The initial states, direct-intent branch, observer effect, hash listener, and mobile launch timer remain at `DeferredEditorLayout.tsx:84-155`; the diff changes none of their logic. The mobile entry has only `{ ssr: false }` (`:44`), and the exclusive selection renders the desktop entry only for `isDesktopEditorLayout === true` (`:157-159`). Coverage verifies passive mobile no-observer behavior (`DeferredEditorLayout.test.tsx:127-136`), mobile hydration (`:138-164`), same-document hash navigation (`:256-274`), cold hash and every supported search parameter (`:298-323`), launch feedback (`:325-339`), no mobile pending fallback (`:341-356`), and timer cleanup (`:358-371`). Independent mobile/SSR review records its focused 6-file/32-test run and ESLint/diff checks at `.superpowers/sdd/desktop-cls-mobile-ssr-review.md:26-49`. |
| 3. SSR and technical SEO are unchanged; no dependency or large refactor | **PASS** | Both dynamic entries still use `ssr: false` (`DeferredEditorLayout.tsx:39-44`); `shouldLoadEditor` starts false (`:84-88`), so SSR retains the existing placeholder shell (`:161-183`) instead of emitting editor client content. `git diff --name-only` contains only `DeferredEditorLayout.tsx` and its corresponding test, so no route, metadata, robots, lockfile, or dependency file changed. The unmodified English and Chinese home pages keep their metadata/canonical declarations and server page order at `src/app/(en)/page.tsx:14-45, 65-73` and `src/app/(zh)/zh/page.tsx:14-45, 65-73`. |
| 4. SRP, KISS, Fail Fast, YAGNI, and precise names | **PASS** | `loadEditorLayout` has only the shared import responsibility (`DeferredEditorLayout.tsx:7-9`); `EditorWorkspaceSkeleton` only renders the reusable inert content (`:11-25`); `DesktopEditorLoadingFallback` only supplies the pending desktop shell (`:27-37`). The conditional in `:157-159` is the smallest exclusive entry-point selection. The diff adds no new abstraction layer, error swallowing, mutable global production state, dependency, or unrelated refactor. |
| 5. Verification material is adequate for a code-contract approval and states the field-data limit | **PASS, with stated measurement boundary** | The current focused layout test run passed 18/18 tests. Recorded integrated validation shows targeted regression tests (3 files/33 tests), `pnpm test` (49 files/297 tests), `pnpm lint`, `pnpm build` (92 static pages), and `git diff --check` all exited 0 at `.superpowers/sdd/desktop-cls-integration-validation.md:10-30`. The prior live audit explicitly records that real layout-shift entries were not measurable in the available browser session at `.superpowers/sdd/desktop-cls-live-audit.md:32-36`. Therefore the evidence proves the fallback/no-null-gap implementation contract, but cannot immediately prove a real-user metric reduction: CrUX CLS is aggregated over a rolling 28-day field-data window. |

## Scope and residual boundary

- Product-code diff scope remains exactly the two planned files; the other untracked files are review/plan artifacts.
- No defect requiring a product-code change was found. **APPROVED.**
- A controlled browser trace can still be run later to observe local `layout-shift` entries and fallback/resolved geometry. That trace would validate an individual lab session, not establish the new 28-day CrUX CLS value.
