1.2 产品定义
Token Maker 是一款面向 TRPG、VTT、GM/DM、地图作者与素材创作者 的在线工具，用于把普通角色、怪物或头像图片，快速制作成可直接用于游戏地图和虚拟桌面的 token。

你是一名资深全栈工程师。请直接开发一个“Token Maker”网站，功能对标 RollAdvantage 的 Token Stamp，但必须使用原创 UI、原创命名、原创资源，不要复制对方品牌名、广告图、页面文案或任何第三方视觉素材。

目标
做一个面向 TRPG / VTT 用户的 Web 应用：用户上传角色/怪物图片后，可以快速制作圆形/方形/多边形 token，支持边框、颜色、文字、遮罩、批量导出，并下载为 PNG。

产品要求
做一个“原创实现、功能相近、交互更现代”的 token maker，桌面端优先，移动端可进行基础操作。
开发要求
使用nextjs tailwindcss shadcn ui技术框架开发

核心功能

1）图片导入与工作区
- 支持拖拽上传和点击选择图片
- 支持 JPG / PNG / WEBP
- 默认显示空工作区提示：“拖拽图片到这里开始”
- 用户导入图片后，图片进入编辑画布
- 支持拖动图片位置
- 支持滚轮缩放图片
- 支持按钮缩放图片
- 支持 Reset Position
- 支持 Clear Workspace
- 所有编辑尽量在前端本地完成，保护隐私

2）画布与导出
- 预览画布默认 512x512
- 导出分辨率默认 1024x1024 PNG
- 导出结果要与预览尽量一致
- 处理透明背景、高 DPI、devicePixelRatio 差异
- 导出时保证文字、边框、遮罩、叠加层都正确合成

3）边框与形状
- 提供一组内置边框模板，使用程序化 SVG/Canvas 生成，不要拷贝第三方资源
- 至少包含：
  - none
  - thin ring
  - thick ring
  - super thin ring
  - square
  - hexagon
  - octagon
  - decagon
  - dodecagon
  - double ring
  - spike ring
  - abstract ring
- 边框模板与遮罩形状解耦：用户可以独立选择边框和 mask
- 默认提供这些 mask：
  - circle
  - square
  - hexagon
  - octagon
  - decagon
  - dodecagon

4）样式控制
- Border Tint（边框色）
- Background Color（背景色）
- Text Color（文字色）
- Overlay Tint（叠加层色）
- Border Opacity（边框不透明度）
- Overlay Opacity（叠加层不透明度）
- Image Scale（图片缩放）
- 所有调节都是非破坏性的，可随时回退和修改
- 提供几个 overlay preset：
  - strong white fade
  - strong fade
  - medium fade
  - soft fade

5）文字功能
- Add Textbox
- 可添加多个文本框
- 文本框支持：
  - 拖动
  - 双击编辑
  - 删除
  - 修改字号
  - 修改字重
  - 修改颜色
  - 修改对齐方式
- 默认文本框位置在底部居中，但可自由移动
- 文字渲染不能糊，导出后依旧清晰

6）自定义资源
- 支持上传自定义 Border
  - 接受 PNG / SVG
  - PNG 需支持透明背景
- 支持上传自定义 Mask
  - 接受 PNG / SVG
  - Mask 采用 alpha/黑白逻辑：白色或不透明区域可见，黑色或透明区域隐藏
- 自定义资源上传后立即可用于当前编辑
- 内置提示说明推荐尺寸，例如 512x512 或更高分辨率

7）批量模式
- 支持批量上传多张角色图
- 批量模式下，当前模板样式（边框、颜色、遮罩、文字样式等）应用到全部图片
- 每张图片生成一个 PNG
- 一键导出 ZIP
- 显示进度条、完成数、失败项
- 失败项要可重试
- 批量模式不要阻塞整个页面，必要时做分批处理或异步队列

8）上传与分享
- Download 按钮必须可用
- Upload 功能做成“存储适配层”
- 默认本地不开启远程上传
- 如果配置了对象存储（如 S3 / Cloudflare R2）环境变量，则自动启用 Upload 按钮
- 上传成功后返回可分享 URL
- README 中写清楚如何配置上传能力

9）状态管理与持久化
- 使用 Zustand 管理编辑器状态
- 当前配置持久化到 localStorage
- 页面刷新后可恢复上次配置
- 提供“恢复默认设置”按钮
- 切换图片时可选择保留当前模板样式

10）UI/UX
- 使用现代暗色风格，不要复刻原站 UI
- 推荐布局：
  - 左侧：控制面板
  - 中间：实时预览画布
  - 右侧：边框模板、遮罩模板、导出区
- 交互要清晰，按钮命名要直观
- 支持中文界面
- 所有文案放到单独的 i18n 字典文件，方便未来扩展英文
- 提供 6~8 个示例头像和几种示例边框，方便开箱即用演示

11）渲染管线
请实现统一的渲染流程，确保预览与导出一致：
1. 绘制背景
2. 根据当前 mask 裁切主图
3. 应用主图位移与缩放
4. 绘制 overlay（带 tint 和 opacity）
5. 绘制 border（带 tint 和 opacity）
6. 绘制文本框
7. 输出最终 PNG

12）性能与工程质量
- 大图导入时要有基本的性能保护
- 避免无意义重复渲染
- 关键模块拆分清晰，不要把所有逻辑塞进一个文件
- 完整 TypeScript 类型定义
- 关键渲染逻辑写注释
- 添加基础测试：
  - 上传图片
  - 切换边框
  - 修改颜色
  - 添加文字
  - 导出 PNG
  - 批量导出 ZIP
- 提供错误处理和用户提示

参考竞品网站https://rolladvantage.com/tokenstamp/，https://heroictoken.com/,https://heroictoken.com/